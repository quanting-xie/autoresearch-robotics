Exp 12 visual analysis (commit 39f01ce)

Quantitative result: eval_success_rate 0.100000, eval_mean_reward -48.900002, eval_mean_distance 0.283670. This regressed from the existing results.tsv best of 0.200000 at commit 6b5cfd7.

Observed behavior:
- Episode 0: the gripper descends toward the block and briefly reaches near it at mid-episode, but by the end the arm has moved away toward the robot base while the block remains on the table, not at the goal.
- Episode 1: the gripper moves to the table surface near the block and then stalls beside/above it. It does not lift, carry, or push the block meaningfully toward the red goal marker.
- Episode 2: the block starts almost adjacent to the goal; the gripper stays close and may create a small contact/push. This appears to be the likely source of the single success, not a general pick-and-place skill.

Comparison to previous best:
- No prior renders_best existed in this checkout before the run, so train.py saved the current mediocre frames as renders_best. The useful comparison is results.tsv: exp10 reached 0.20 success with N_UPDATES=3 and AT_GOAL_BONUS=20, while this run with N_UPDATES=4 and AT_GOAL_BONUS=5 fell to 0.10 and worse mean distance.

Hypothesis assessment:
- The hypothesis that reducing AT_GOAL_BONUS from 20 to 5 would reduce jerkiness while preserving success was not confirmed. The lower bonus removed too much incentive for final placement, and the policy mainly approaches or hovers around the block instead of completing transport to the goal.

Visible failure modes:
- Reaches near the object but fails to close/grasp consistently.
- Little or no block displacement in episodes where the block is not already near the goal.
- Success appears dependent on easy initial goal/block geometry rather than robust manipulation.
- The high replay ratio plus lower terminal bonus may over-optimize shaped approach behavior without enough final-goal pressure.

Next suggestion:
- Restore AT_GOAL_BONUS to 20 and revert N_UPDATES from 4 back to the proven N_UPDATES=3 baseline, then try a smoother final-placement reward instead of a lower flat bonus: add a small gaussian bump around the goal or increase goal shaping locally only when the gripper is near the block. This keeps the exp10 metric anchor while attacking the human-observed jerkiness more surgically.

Exp 13 visual analysis (commit 0f52eeb)

Quantitative result: eval_success_rate 0.000000, eval_mean_reward -50.000000, eval_mean_distance 0.289518. This regressed below the existing best of 0.200000 at commit 6b5cfd7 and below exp12's 0.100000.

Observed behavior:
- Episode 0: the gripper reaches table height near the block and goal marker but does not close around the block or push it to the goal. The block remains essentially stationary.
- Episode 1: the gripper starts near the block, then hovers or stalls above it. There is no visible lift, carry, or sustained pushing motion.
- Episode 2: the arm moves far across the table while the block remains behind. The goal marker is not reached by the object.

Comparison to previous best:
- No renders_best directory was present for direct image comparison. Relative to results.tsv, this run is a clear regression from exp10's 0.20 success and mean distance 0.147.

Hypothesis assessment:
- The hypothesis that damping the velocity bonus inside 10 cm of the goal would reduce jerkiness while preserving success was not confirmed. Removing the near-goal velocity incentive appears to have weakened the already fragile contact/transport behavior, producing no successes.

Visible failure modes:
- Approach without reliable grasp or push.
- Object remains stationary in all sampled rollouts.
- Policy sometimes moves the arm away from the object after reaching the table.
- Damping the velocity term near the target removes useful shaping before the policy has learned stable transport.

Next suggestion:
- Discard the velocity-damping change and restore exp10 reward shaping. The next experiment should preserve the full velocity bonus and instead improve contact acquisition earlier in the trajectory, for example by rewarding gripper closure only when the gripper is close to the block.

Exp 14 visual analysis (commit 82df6c2)

Quantitative result: eval_success_rate 0.000000, eval_mean_reward -50.000000, eval_mean_distance 0.283366. This failed to recover the exp10 best of 0.200000.

Observed behavior:
- Episode 0: the gripper descends close to the block and may make light contact, but the block is not moved to the goal.
- Episode 1: the gripper moves toward the object side of the table, then ends down by the table edge instead of transporting the block.
- Episode 2: the arm extends away from the block for much of the rollout; the block remains near its starting point and never reaches the goal.

Comparison to previous best:
- No renders_best directory is available for direct image comparison. Relative to results.tsv, the contact-close bonus did not preserve the exp10 success rate and only slightly improved mean distance over exp13.

Hypothesis assessment:
- The hypothesis that a small close-command bonus near the block would improve contact was not confirmed. It encouraged gripper posture changes near the object, but did not produce reliable pushing, lifting, or goal-directed transport.

Visible failure modes:
- Contact, when present, is weak and not aligned with the goal.
- The policy can drive the gripper off the table edge after approaching.
- The block remains stationary or only minimally displaced.
- Action-dependent close shaping adds another local behavior without solving transport.

Next suggestion:
- Discard the contact-close bonus and restore exp10 reward shaping. The next clean test should isolate N_UPDATES=4 with AT_GOAL_BONUS=20, because exp12 confounded higher replay ratio with a weakened terminal bonus.

Exp 15 visual analysis (commit 7772d58)

Quantitative result: eval_success_rate 0.100000, eval_mean_reward -45.000000, eval_mean_distance 0.267965. This partially recovers success relative to exp13/14 but remains below the exp10 best of 0.200000.

Observed behavior:
- Episode 0: the gripper moves to the table edge near the block and goal marker but does not push the block to the target.
- Episode 1: the gripper approaches the side of the block/table and then stays low beside the table; the block remains on the table away from the goal.
- Episode 2: the arm moves back toward the robot base while the block and goal remain on the table with little visible progress.

Comparison to previous best:
- No renders_best directory is available. Relative to exp10 in results.tsv, RR=4 uses fewer environment interactions in the fixed time budget and does not match the 0.20 success rate.

Hypothesis assessment:
- The hypothesis that N_UPDATES=4 would improve value fitting when AT_GOAL_BONUS stays at 20 was not confirmed. It recovered one success, showing exp12 was partly confounded by the low at-goal bonus, but the higher replay ratio still underperformed RR=3.

Visible failure modes:
- Goal-directed transport remains weak.
- The gripper often reaches the object side of the table without producing useful displacement.
- Training showed early critic/actor magnitude spikes around 12k-18k steps.
- Fewer environment episodes under RR=4 likely reduce useful exploration within the 30 minute budget.

Next suggestion:
- Restore N_UPDATES=3 and preserve the exp10 reward anchor. Instead of rewarding arbitrary block displacement alone, add a capped positive bonus for reducing block-goal distance between consecutive steps so pushes are directed toward the target.

Exp 16 visual analysis (commit ae0e2f0)

Quantitative result: eval_success_rate 0.000000, eval_mean_reward -50.000000, eval_mean_distance 0.254525. Mean distance improved relative to recent failures but sparse success fell to zero.

Observed behavior:
- Episode 0: the arm moves away from the block after start; the block remains near but not on the goal.
- Episode 1: the gripper descends around the block/goal area but ends beside the block without placing it at the target.
- Episode 2: the block starts close to the goal marker, but the gripper moves off to the side and the object stays just outside success.

Comparison to previous best:
- No renders_best directory is available. Relative to exp10, final distances are closer than some failed runs but not enough to cross the fixed 0.05 success threshold.

Hypothesis assessment:
- The hypothesis that a capped goal-progress bonus would make transport more goal-directed was not confirmed. It improved mean distance but did not create successful placements and appears to leave the policy satisfied with near-misses.

Visible failure modes:
- Near-goal states remain just outside the success threshold.
- The gripper often moves away after getting close.
- Object motion is still inconsistent; the local progress signal is not enough to finish placement.
- Late critic loss spikes suggest the added temporal progress reward increased value noise.

Next suggestion:
- Discard the progress bonus and restore exp10 shaping. Try a smoother local final-placement reward around the goal instead of a stepwise progress term, keeping AT_GOAL_BONUS=20 as the hard success incentive.

Exp 17 visual analysis (commit 3b9051a)

Quantitative result: eval_success_rate 0.000000, eval_mean_reward -50.000000, eval_mean_distance 0.375917. This is a major regression from the exp10 best and from exp16's mean distance.

Observed behavior:
- Episode 0: the arm retracts upward/back toward the robot while the block remains on the table.
- Episode 1: the gripper descends near the table but does not move the block; by the end the object is still far from the red marker.
- Episode 2: the arm lifts high above and away from the table, leaving the block stationary.

Comparison to previous best:
- No renders_best directory is available. Compared with exp10 and exp16, the smooth local bonus worsened both sparse success and final distance.

Hypothesis assessment:
- The hypothesis that a Gaussian near-goal bonus would convert near-misses into successes was not confirmed. The bonus inflated value targets, produced sustained high critic losses, and the policy avoided useful contact.

Visible failure modes:
- Arm retracts or lifts away from the object.
- Block remains stationary in sampled rollouts.
- Mean distance worsens substantially.
- Critic loss stays high for much of training, indicating the bonus scale is too large for the current recipe.

Next suggestion:
- Remove the local Gaussian bonus and restore exp10 shaping. If increasing final-placement pressure, prefer a narrower change to the existing hard at-goal bonus rather than adding broad positive dense reward.

Exp 18 visual analysis (code commit 65db451)

Quantitative result: eval_success_rate 0.200000, eval_mean_reward -40.200001, eval_mean_distance 0.214567. This ties the existing best success rate of 0.200000 and slightly improves mean reward over exp10, but does not beat the best success metric.

Observed behavior:
- Episode 0: the gripper reaches down to the block and stays in contact or near-contact, but the block is not moved to the red goal marker.
- Episode 1: the gripper initially approaches near the object, then swings far away from the table by the end while the block stays put.
- Episode 2: the gripper reaches the object side of the table and may brush the block, but the object remains off target.

Comparison to previous best:
- The scalar success rate ties exp10. The run used the newer sparse-reward/early-stop setup with N_UPDATES=1, 2-hour ceiling, and spot-eval stopping, and stopped early after no-progress despite one spot eval reaching 0.20. Visuals are not qualitatively better than exp10.

Hypothesis assessment:
- This external early-stop/sparse setup is viable enough to tie the best success rate, but the visual behavior still lacks robust goal-directed transport. Because success did not exceed 0.20, the existing best commit should remain unchanged.

Visible failure modes:
- Successes are not visible in the three rendered rollouts.
- The gripper still often moves away after approach.
- Block displacement remains weak and inconsistent.
- Spot evals were unstable: 0.00, 0.00, 0.20, 0.00, 0.00, 0.00.

Next suggestion:
- Treat the sparse early-stop setup as promising infrastructure, but do not replace the best metric anchor yet. A useful next test would reintroduce the proven exp10 shaping terms on top of the early-stop/long-ceiling infrastructure, then compare whether spot-eval stability improves.

Exp 19 visual analysis (commit 88dc11b)

Quantitative result: eval_success_rate 0.000000, eval_mean_reward -50.000000, eval_mean_distance 0.215355. Training stopped after 1200.1s at 39798 steps with early_stop_reason "no-progress - 600s elapsed, last 3 spot-evals all 0". Spot eval history was [0.20, 0.00, 0.00, 0.00].

Observed behavior:
- Episode 0: the arm starts over the block, then extends away from the table; the block remains stationary and off target.
- Episode 1: the gripper hovers near the object and goal region, but does not move the block to the red marker.
- Episode 2: the gripper retreats behind/above the robot side of the table while the block and goal stay separated.

Comparison to previous best:
- This is a clear scalar regression from both exp10 and exp18, which each reached 0.20 success. Mean distance is similar to exp18, but final sparse success collapsed to zero and sampled rollouts show no useful transport.

Hypothesis assessment:
- The hypothesis was not confirmed. The full exp10 restore did not stabilize or improve the early-stop runner. It produced one early 0.20 spot eval, followed by three zero spot evals and no-progress termination.

Visible failure modes:
- Spot-eval instability followed by collapse.
- Critic loss climbed into the 300-400 range during early training.
- The gripper hovers near the block or retreats, rather than maintaining useful contact.
- Block displacement remains absent in sampled rollouts.

Next suggestion:
- Discard the full exp10 restore and return to the sparse early-stop baseline. If continuing, isolate factors one at a time, starting with N_UPDATES=2 or a single lightweight shaping term rather than replay ratio, dense rewards, LayerNorm, and target clamp together.
